{
  "name": "vesty-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm --workspaces run build",
    "typecheck": "npm run typecheck --workspace vesty-plugin-ui",
    "test": "npm --workspaces run test"
  }
}
